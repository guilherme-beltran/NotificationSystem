import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'NotificationSystem.Client',
  webDir: 'www'
};

export default config;
